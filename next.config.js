/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['tailwind-nextjs-starter-blog.vercel.app', 's3.us-west-2.amazonaws.com'],
    },
}

module.exports = nextConfig
